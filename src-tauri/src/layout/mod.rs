//! Layout engine: zone/item persistence, resolution handling, and snapshots.

pub mod persistence;
pub mod resolution;
pub mod snapshot;

// Re-export core types for convenient access from other modules.
